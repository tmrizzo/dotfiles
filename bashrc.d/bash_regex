# ~/.bash_regex
# Bash file dedicated to regular expression functions

# Delete all lines beginning with a # from a file
rg_comments ()
{
  egrep -v '^\s+?#|^$' $1
}

# Filter iostat 10th column for values greater than 10
rg_iosvctm ()
{
  awk '/^(sd|dm-)/ { if ( $10 > 10 ) print $0 }' $1
}
